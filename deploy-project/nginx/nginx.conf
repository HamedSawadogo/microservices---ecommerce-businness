events {}

http {
    upstream backend_cluster {
        least_conn;
        server ecom-backend:9090;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend_cluster;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}